{
  "name": "@parabains-bot/standard-classifier",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "yarn clean && webpack --config webpack.prod.js",
    "clean": "rm -rf dist",
    "deploy": "./scripts/deploy.sh",
    "notebook": "docker-compose up",
    "start": "webpack --config webpack.dev.js --progress"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^1.7.2"
  },
  "devDependencies": {
    "@parabains-bot/webpack": "1.0.0"
  }
}
